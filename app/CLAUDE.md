# App (Next.js App Router)

Next.js 15 App Router routes, layout, metadata emitters (OG, manifest, sitemap, robots), and Serwist service worker entry.

See context/kits/ for requirements this code implements.
See context/plans/ for task dependency graphs.
See DESIGN.md (project root) before modifying any user-facing component.
