# Components

Shared UI components (Nav, Footer, CTA, Artifact*). Imported by app/ routes.

See context/kits/ for requirements this code implements.
See context/plans/ for task dependency graphs.
See DESIGN.md (project root) — components must reference tokens and patterns from it.
